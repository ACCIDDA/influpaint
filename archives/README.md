# Archives

Historical experiments and reference material. The active paper workflow is described in [Walkthrough → Start here](../docs/workflows/start-here.md).

| Path | Contents |
| --- | --- |
| `trajectory-to-flusight-converter/` | Standalone trajectory-converter experiment |
| `synthetic_dataset/` | Earlier synthetic-data generation experiments and local outputs |
| `dev/` | Early diffusion experiments and reference notebooks |
| `referenceimplementations/` | Local third-party reference checkouts (excluded from Git) |
| `*.ipynb` | Historical operational influenza and North Carolina forecasting notebooks |
| `evaluate-deprecated.py` | Earlier evaluation implementation |
| `wnv-forecasting/` | Local historical West Nile virus work |

Archived notebooks preserve their original code and outputs and may depend on historical environments or data paths.

`CoPaint4influpaint/` remains at the repository root because it is an active runtime dependency of the paper and satellite applications.
